import MainTitle from "../Components/MainTitle/MainTtitle";

function Contact() {
  const titleRows = ["Let's work together.", "Get in touch."];

  return <MainTitle titleRows={titleRows} />;
}

export default Contact;
